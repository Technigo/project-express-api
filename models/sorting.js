// not used but saved for future reference

// const sorting = ( arr, key, direction = asc ) => {
// 	if(direction === asc) {
// 		return arr.sort((a, b) => (a[key] > b[key]) ? 1 : ((b[key] > a[key]) ? -1 : 0))
// 	} else {
// 		return arr.sort((a, b) => (a[key] < b[key]) ? 1 : ((b[key] < a[key]) ? -1 : 0))
// 	}
// }

// const sortedByAuthorAsc = sorting(books, author)
// const sortedByTitleDsc = sorting(books, author, desc)
// const sortedByPagesAsc = sorting(books, author, asc)

// export default sorting;
